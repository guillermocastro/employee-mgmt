import { Injectable } from "@angular/core";

export class Employee {
    //Automatically will generate a key
    $key:string;
    name:string;
    position:string;
    office:string;
    salary:number;
}
